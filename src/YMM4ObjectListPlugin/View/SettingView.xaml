<UserControl
  x:Class="ObjectList.View.SettingView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
  xmlns:core="clr-namespace:ObjectList"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:ObjectList.View"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:viewmodels="clr-namespace:ObjectList.ViewModel"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d"
  >
  <ScrollViewer
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    HorizontalScrollBarVisibility="Disabled"
    VerticalScrollBarVisibility="Auto"
    >
    <StackPanel>
      <Expander Header="YMM4 オブジェクトリスト プラグイン" IsExpanded="True">
        <Grid Margin="10">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <Label
            Grid.Row="0"
            Grid.Column="0"
            Content="Plugin version"
            />
          <TextBox
            x:Name="PluginVersion"
            Grid.Row="0"
            Grid.Column="1"
            IsReadOnly="True"
            Text="{Binding PluginVersion, Mode=OneWay}"
            />

          <Label
            Grid.Row="1"
            Grid.Column="0"
            Content="{Binding UpdateMessage}"
            IsEnabled="{Binding HasUpdate}"
            />
          <Button
            x:Name="UpdateCheck"
            Grid.Row="1"
            Grid.Column="1"
            Margin="2"
            Command="{Binding UpdateCheck}"
            Content="Update check"
            IsEnabled="{Binding IsUpdateCheckEnabled}"
            />

          <Button
            x:Name="Download"
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="2"
            Command="{Binding Download}"
            Content="Download"
            IsEnabled="{Binding IsDownloadable}"
            />
        </Grid>
      </Expander>


      <!--  options  -->
      <Expander Header="オプション" IsExpanded="True">
        <StackPanel>
          <!--  列ヘッダーオプション  -->
          <GroupBox Name="ColumnHeaderOptions" Margin="5">
            <GroupBox.Header>
              <DockPanel>
                <Label Content="列ヘッダー" />
              </DockPanel>
            </GroupBox.Header>

            <StackPanel Margin="5" IsEnabled="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}">

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="🎨アイテムカラー列を表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="false" />
                <Label Content="🗃️カテゴリ列を表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="#️⃣レイヤー番号列を表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="🧱レイヤー名列を表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="#️⃣グループ番号列を表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="👥グループ名列を表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="🔒ロック列を表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="👁️非表示列を表示" />
              </DockPanel>

            </StackPanel>
          </GroupBox>


          <!--  フッターオプション  -->
          <GroupBox Name="FooterOptions" Margin="5">
            <GroupBox.Header>
              <DockPanel>
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="フッターを表示" />
              </DockPanel>
            </GroupBox.Header>

            <StackPanel Margin="5" IsEnabled="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}">

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="シーン名を表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="false" />
                <Label Content="シーンの画面サイズを表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="シーンFPSを表示" />
              </DockPanel>

              <DockPanel Margin="0,2">
                <c:ToggleSlider DockPanel.Dock="Right" Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}" />
                <Label Content="シーンの音周波数を表示" />
              </DockPanel>

            </StackPanel>
          </GroupBox>


          <Grid Margin="10">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label
              Grid.Row="0"
              Grid.Column="0"
              Content="フッターを表示"
              />
            <c:ToggleSlider
              Grid.Row="0"
              Grid.Column="1"
              HorizontalAlignment="Right"
              Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}"
              />

            <Label
              Grid.Row="1"
              Grid.Column="0"
              Content="ボイスライブラリ切替高速化（※時間がかかります）"
              ToolTip="VoiSona Talkは別トラックにボイスを割り当てていると速くボイス切替ができます。事前に利用しないトラックにすべてのボイスを割り当てます。"
              />
            <c:ToggleSlider
              Grid.Row="1"
              Grid.Column="1"
              HorizontalAlignment="Right"
              Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}"
              />

            <Label
              Grid.Row="2"
              Grid.Column="0"
              Content="{Binding UpdateMessage}"
              IsEnabled="{Binding HasUpdate}"
              />
            <c:ToggleSlider
              Grid.Row="2"
              Grid.Column="1"
              HorizontalAlignment="Right"
              Value="{Binding Source={x:Static core:ObjectListSettings.Default}, Path=IsShowFooter}"
              />

          </Grid>
        </StackPanel>
      </Expander>



      <GroupBox
        Margin="10"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        Header="YMM4 オブジェクトリスト プラグインについて"
        >
        <StackPanel>
          <TextBlock Margin="5" TextWrapping="Wrap">VoiSona Talkに対応した非公式プラグインです。</TextBlock>
          <TextBlock Margin="5" TextWrapping="Wrap">このプラグインが原因のエラーはYMM4本体の開発者ではなく、いぬいぬ（InuInu2022）にご連絡ください。</TextBlock>
          <TextBlock Margin="5">Ⓒ InuInu 2025 -</TextBlock>
          <Button
            x:Name="GoToGithub"
            Command="{Binding OpenGithub}"
            Content="Open Github"
            />
        </StackPanel>
      </GroupBox>


    </StackPanel>
  </ScrollViewer>
</UserControl>
